#include <stdio.h>
void printOdds(int a, int b)
{
  int i;
  for(i=a;i<=b;i++)
  {
    if(i%2==1)
    {
      printf("%d ",i);
    }
  }
}
int main()
{
  int a,b;
  scanf("%d%d", &a,&b);
  printOdds(a, b);
  return 0;
}
