#include <stdio.h>
#include <limits.h>
#include <stdbool.h>
#include <stdlib.h>
#include <math.h>
long fact(n) 
{
  if (n == 0)  
    return 1;  
  else  
    return(n * fact(n-1));  
}
int main()
{
   int n;
   scanf("%d",&n);
   printf("%ld", fact(n));
}
